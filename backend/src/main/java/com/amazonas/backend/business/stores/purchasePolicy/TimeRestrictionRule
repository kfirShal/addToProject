public class TimeRestrictionRule implements PurchaseRule {
    private final int hourLimit;

    public TimeRestrictionRule(int hourLimit) {
        this.hourLimit = hourLimit;
    }

    @Override
    public boolean isSatisfied(Cart cart, User user) {
        return LocalTime.now().getHour() < hourLimit;
    }
}