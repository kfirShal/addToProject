public class AndRule implements PurchaseRule {
    private final List<PurchaseRule> rules;

    public AndRule(List<PurchaseRule> rules) {
        this.rules = rules;
    }

    @Override
    public boolean isSatisfied(Cart cart, User user) {
        return rules.stream().allMatch(rule -> rule.isSatisfied(cart, user));
    }
}