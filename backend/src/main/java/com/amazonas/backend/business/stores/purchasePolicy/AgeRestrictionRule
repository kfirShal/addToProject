public class AgeRestrictionRule implements PurchaseRule {
    private final int minAge;

    public AgeRestrictionRule(int minAge) {
        this.minAge = minAge;
    }

    @Override
    public boolean isSatisfied(Cart cart, User user) {
        return user.getAge() >= minAge;
    }
}